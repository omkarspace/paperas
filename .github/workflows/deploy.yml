name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t paperas .

      - run: echo "Add your deployment target here (VPS, Vercel, Railway, etc.)"
